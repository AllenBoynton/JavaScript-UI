// Allen Boynton
// MIU 1506
// Informational App
// June 18, 2015

// Set backgroung 
var mainWindow = Ti.UI.createWindow ({
	backgroundImage: "images/grass.jpg",
	title: "Top 20 Strongest Dog Breeds"
});

var navWindow = Ti.UI.iOS.createNavigationWindow ({
	window: mainWindow
});
 
// Create a Button.
var window1 = Ti.UI.createButton({
	title : 'window1',
	height : myHeight,
	width : myWidth,
	top : myTop,
	left : myLeft
});

// Listen for click events.
window1.addEventListener('click', function() {
	alert('\'window1\' was clicked!');
});

// Add to the parent view.
parentView.add(window1);


// var loadFile = require("json.js");
